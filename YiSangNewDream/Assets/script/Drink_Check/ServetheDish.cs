using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ServetheDish : MonoBehaviour
{
    // Start is called before the first frame update
    //这个按钮绑定到按钮上面，如果按下就移动摄像头
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
